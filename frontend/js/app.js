/**
 * Состояние приложения (перемещено из встроенного скрипта)
 */
const appState = {
  marketplace: 'ozon',
  env: 'sandbox',
  lastPayload: null,
  logs: [],
};

// Просто переместить всю логику из встроенного скрипта сюда
// (будет создан в основном файле)
